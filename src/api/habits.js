// axios 설치 후 주석 해제
// import api from "./instance";

// 오늘의 습관 조회
// export const getTodayHabits = (studyId) => api.get(`/studies/${studyId}/habits/today`);

// 습관 생성
// export const createHabit = (studyId, body) => api.post(`/studies/${studyId}/habits`, body);

// 습관 체크/해제
// export const checkHabit = (studyId, habitId, body) => api.patch(`/studies/${studyId}/habits/today/${habitId}`, body);

// 습관 수정
// export const updateHabit = (studyId, habitId, body) => api.patch(`/studies/${studyId}/habits/${habitId}`, body);

// 습관 삭제
// export const deleteHabit = (studyId, habitId) => api.delete(`/studies/${studyId}/habits/${habitId}`);

// 주간 습관 기록 조회
// export const getHabitLogs = (studyId, date) => api.get(`/studies/${studyId}/habit-logs`, { params: { date } });
